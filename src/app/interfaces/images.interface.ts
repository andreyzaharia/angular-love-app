export interface Imagen {
  nombre: string;   // Nombre del archivo o ruta relativa
  url: string;      // URL completa de descarga desde Firebase
  description: string;      // URL completa de descarga desde Firebase
  km: string;
}
